"
I am an abstract error that can be thrown by the project.

All other errors in the project should inherit from myself.
"
Class {
	#name : #SQLite3AbstractError,
	#superclass : #Error,
	#category : #'SQLite3-Errors'
}
